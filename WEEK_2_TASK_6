import java.io.*;
import java.util.*;

// Read only region start
class UserMainCode
{
    public int allDigitsCount(int input1){
        // Read only region end
        if(input1 <= 0 && input1 > 25000){
            throw new UnsupportedOperationException("allDigitsCount(int input1)");
        }

        String str = String.valueOf(input1);
        int n = str.length();

        return n;
    }
}
